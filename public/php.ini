; Allow the script to run for 10 minutes (600 seconds)
max_execution_time = 600

; Increase memory limit if PHP is processing the entire CSV into an array
memory_limit = 512M

; Ensure the file upload itself isn't blocked
post_max_size = 50M
upload_max_filesize = 50M

max_input_vars = 15000